# Leader vs Broad Participation Report

- `RS` full-book positive day PnL share splits as leader-dominant `48.97%`, broad-participation `35.74%`, and mixed/unclear `15.29%`.
- `CSM` full-book positive day PnL share splits as leader-dominant `50.80%`, broad-participation `31.93%`, and mixed/unclear `17.27%`.
- Broad-participation-only performance is weak relative to the full books but not zero: `RS` reaches `$34369.33` (`37.48%`) and `CSM` reaches `$27269.38` (`9.08%`).
- `RS` is cleaner on broad-participation days overall. Drawdown is `19.62%` for CSM versus `21.89%` for RS, but concentration is much worse for CSM: top-symbol share is `90.30%` for CSM versus `53.55%` for RS.
- Neither upside branch shows a clean broad-participation core. The broad-participation slice still fails the diversification smell test, especially for `CSM` where top-symbol PnL share stays `90.30%`.
- `DSE` remains structurally cleaner under this lens but not stronger: broad-participation-only final equity is `$24798.46` on only `3` trades, which keeps it as the benchmark/control rather than the upside branch.
